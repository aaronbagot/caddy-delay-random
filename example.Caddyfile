{
	order delay_random before reverse_proxy
}

http://localhost:8000 {
    delay_random 500ms 10s 0.8
	reverse_proxy https://icanhazip.com {
		header_up Host {upstream_hostport}
	}
	log {
		format console
	}
}